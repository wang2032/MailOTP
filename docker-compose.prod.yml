services:
  mailotp:
    image: ${DOCKER_IMAGE:-harbor.10rig.com:8443/apps/mailotp:latest}
    restart: unless-stopped
    ports:
      - "18080:8080"
    environment:
      DATABASE_URL: "${DATABASE_URL}"
      REDIS_URL: "${REDIS_URL}"
      WEBHOOK_SECRET: "${WEBHOOK_SECRET}"
      INBOX_CREATE_PASSWORD: "${INBOX_CREATE_PASSWORD}"
      MAIL_DOMAIN: "${MAIL_DOMAIN}"
      HTTP_ADDR: ":8080"
      STATIC_DIR: "/app/web"
      AUTO_CREATE_TABLES: "true"
      CORS_ORIGINS: "${CORS_ORIGINS}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
